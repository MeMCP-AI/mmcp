# context7 Feature Requests

### FR-001: More code snippets for niche crates (2026-04-16)

**Need**: Searched for `axum-login` docs and only got 2 snippets (a changelog entry and a Cargo.toml line). For crates with small doc surfaces, the tool isn't much more useful than my training data. More complete API reference extraction would help.

### FR-002: Version-specific queries (2026-04-16)

**Need**: When using `query-docs`, there's no way to pin to a specific crate version. For axum-login 0.18 vs 0.17, the API changed significantly. Version-aware results would prevent outdated advice.
